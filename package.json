{
  "name": "loudtogether",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "concurrently": "^6.2.0"
  }
}
